// Read-only lead options are exposed through the Leads module so every role
// that can work with leads can load the dropdowns without Settings access.
export { GET } from "../../settings/leads/route";
