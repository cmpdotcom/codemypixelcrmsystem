/** Fallback logo used everywhere a company logo has not been uploaded. */
export const DEFAULT_LOGO = "/defaultlogo.png";

/** Browser tab icon generated from the default logo. */
export const DEFAULT_FAVICON = "/favicon.ico";
